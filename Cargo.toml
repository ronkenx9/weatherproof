[package]
name = "weatherproof-scorer"
version = "0.3.0"
edition = "2021"
license = "MIT"
description = "A deterministic WEATHER_FORECAST scorer for Telegraph Protocol"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true

[dependencies]
